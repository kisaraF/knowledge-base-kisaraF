# Mutable, unordered data structure that prohibits duplicate values

myset = {1,1,3,5,2,51,1,5} # removes duplicates
print(myset)

#----- Creating an empty set
empty_set = set()

# --- Mutating by adding new

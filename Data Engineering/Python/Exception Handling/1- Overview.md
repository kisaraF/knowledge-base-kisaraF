# Exception Handling

In Python exception handling is used to deal with problems that could fail/ break the code when unxpected. 

In Python, an `exception` itself, is a special object that Python uses to manage errors.

A `traceback` is the report of the exception that was raised.